import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ar.com.ludogramming.icarus',
  appName: 'icarus-mobile',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
